export function SEE_SAGA() {
    // This function is used as an action handler placeholder indicating that the action is dealt
    // with inside a saga (or multiple sagas).
}

export function NOOP() {
    // In case we want a constant empty function, here it is. Enjoy. <3
}
